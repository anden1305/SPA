#!/bin/bash
# DEPRECATED — use submit_ablation_chmm_seq_length_compare.sh (9 jobs, winner recipe only).
# This script submitted the full 12-variant matrix per sequence length.
echo "Use: bash hpc/submit/cv4fold/submit_ablation_chmm_seq_length_compare.sh" >&2
exit 1
